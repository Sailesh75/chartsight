# Python
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# Virtual environment (never commit installed packages)
.venv/
venv/
env/

# Local AWS / secrets
.env
.aws/

# OS / editor
.DS_Store
.vscode/
.idea/

# Claude Code local settings
.claude/settings.local.json

# Eval run output — regenerated by `python -m evals.run` / CI; gold.jsonl (the
# dataset itself) IS committed so the gold set is reviewable and reproducible.
evals/report.md
evals/report.json
